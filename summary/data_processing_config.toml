DB_PATH = "compact_db"
DB_SIZE = 44_000_000_000

DATA_PATH = "../anki-revlogs-10k"

USER_START = 1000
USER_END = 2000

DTYPE = "float"
PROCESSES = 5

# The memory usage is at most MAX_TOTAL_SIZE * memory_per_review
# Should be optimized to fit the device VRAM constraint when training
MAX_TOTAL_SIZE = 6_000_000

# The memory usage is at most MAX_FACTOR * len(revlog) * memory_per_review
# Should be optimized for throughput and storage size
# For CPU usage the optimal value would likely be smaller
MAX_FACTOR = 20
